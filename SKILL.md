---
name: kuajing-wulaoshi-amazon-category-sales-trend
description: 生成“跨境吴老师”品牌的 Amazon 叶子类目或用户确认最细匹配类目的近 24 个自然月销量趋势离线 HTML 看板。用于用户提供商品名称与 Amazon 站点后，需要通过其本地配置的 SellerSprite／卖家精灵 MCP 发现高度相关类目、采集完整类目月度 totalUnits，并交付自包含趋势看板时；也用于类目节点、叶子验证、用户确认类目、月度完整类目销量、部分月份缺失、MCP 连接选择或离线 HTML 趋势看板任务。不得用于关键词趋势、ASIN 销量趋势或 Top 100 商品样本替代完整类目销量。
---

# 跨境吴老师 Amazon 类目销量趋势

生成单文件、离线可打开的 HTML 看板。保留“跨境吴老师”名称和固定紫粉视觉。不要输出 Markdown 看板、原始数据表或文字版结论替代 HTML。

## 启动与输入

当用户仅启动本 Skill 时，回复：

```text
跨境吴老师正在准备类目销量趋势分析模板...

请提供以下信息后开始分析：
1. 商品名称
2. Amazon 站点代码：US、UK、AU、CA、JP、DE、FR、IT、ES、MX、BR、IN、AE

示例：商品名称：示例商品；站点：US
```

在商品名称与有效站点均齐全前，不调用 MCP、不生成 HTML。将站点转为大写。遇到无效站点，只请求有效站点。

若商品名只有品牌、型号或无法区分核心商品与配件／耗材，先追问商品类型、用途或适用对象。不要以销量高低猜测商品意图。

## 跨境吴老师品牌化执行提示

在真实发生下列状态转换时，输出对应的一条用户可见提示；不重复刷屏，也不虚构已完成的操作。

- 输入齐全后、开始多步骤流程前：`跨境吴老师正在准备类目销量趋势分析模板...`
- 开始发现或绑定 MCP 前：`跨境吴老师正在检查卖家精灵 MCP 连接...`
- 开始搜索、核验或等待用户选择类目前：`跨境吴老师正在定位并确认目标类目...`
- MCP 不能验证叶子、需要用户确认候选路径时：`跨境吴老师正在等待您确认目标类目路径...`
- 开始逐月采集完整类目数据前：`跨境吴老师正在获取近24个月完整类目销量数据...`
- 开始写入离线文件前：`跨境吴老师正在生成类目销量趋势看板...`
- 阻塞时：`跨境吴老师当前无法继续类目销量趋势分析：<事实原因>。`
- 成功交付时：`跨境吴老师类目销量趋势看板已完成：<HTML 文件链接>`

输入收集、连接选择和类目确认提示不生成 HTML。最终成功消息只保留上述完成语和 HTML 文件链接。

## MCP 连接与能力绑定

先读取 [数据契约与判定规则](references/data-contract.md)。只检查当前运行时已暴露的 MCP 工具；不要写死服务名、命名空间、URL、密钥、账号或后缀。

1. 将候选工具按 MCP 服务分组，确认服务确实提供卖家精灵／SellerSprite 数据能力。
2. 在同一组中验证下列业务能力、核心入参和输出语义：`product_node`、`market_research`，以及仅在类目语义仍有歧义时使用的 `competitor_lookup`。
3. 仅一组通过验证时，绑定该组；多组通过验证时，向用户展示运行时发现的服务标识，请其选择本次任务使用的连接。不得按序号、名称或历史环境自行选择。
4. 绑定后只使用所选组。能力、参数或返回结构无法确认时，停止并说明配置条件；不得换用关键词、ABA、Google Trends、ASIN 销量或 Top 100 商品数据。

同一任务内，对相同连接、站点、月份、类目路径和字段集合的请求复用已确认响应；串行调用，避免并发。仅遵循工具说明或响应中明确给出的限流／重试指示。不要把自定频率伪装成卖家精灵官方调用上限。

## 类目选择

1. 根据站点和商品名生成核心商品类型搜索词。英文站点使用英文；JP、DE、FR、IT、ES/MX、BR、AE 按目标市场语言处理；品牌、型号和专有名词不强行翻译。最多两轮收窄改写。
2. 以 `product_node` 查找候选，最多保留 8 个高度相关节点。保留节点路径、名称、商品数和实际可用的叶子标识。
3. MCP 明确返回 `isLeaf=true` 时，认定为“已验证叶子类目”；若绑定工具的文档明确支持“查询直接下级节点”，才可用其无下级结果验证叶子。不得因路径处于最末级、商品数、空数组或名称猜测叶子。
4. 排除错误落入的配件、替换件、耗材、补充液、收纳件或关联品类；但用户商品本身属于这些类型时，不得误排除。
5. 多个候选仍有语义歧义时，使用 `competitor_lookup` 的少量商品标题与精确类目筛选作意图核验。不要请求或使用其销量、销售额替代类目月度销量。
6. 多个“已验证叶子类目”语义同等相关且均能提供同一截止月的完整 `totalUnits` 时，选择 `totalUnits` 最高者。若不能作同口径比较，向用户澄清，不要混用平均销量、销售额、商品数或其他指标排序。
7. 若最匹配候选无法由 MCP 验证叶子，不能直接阻断或自动按销量选择。展示候选的英文／本地路径和 `nodeIdPath`，说明“SellerSprite MCP 未提供叶子验证”，并要求用户明确确认“按此类目分析”。仅在收到确认后，将该节点标为“用户确认的最细匹配类目（MCP 未提供叶子验证）”；确认前不得调用 `market_research` 采集销量或生成 HTML。用户拒绝、未选择或仍有语义歧义时，继续澄清。

## 月度数据与部分覆盖

以执行时区的上一个自然月作为目标截止月。构造截至该月的 24 个连续自然月，MCP 月份使用 `yyyyMM`，页面使用 `YYYY-MM`。

对“已验证叶子类目”或“用户确认的最细匹配类目”的最终 `nodeIdPath`，逐月调用 `market_research`。只接受与目标路径完全一致且能唯一确认的记录。保存 `totalUnits`、`avgUnits`、`avgRevenue`、`avgPrice`、`totalProducts` 和该月状态。

- `totalUnits` 是唯一销量主指标。0 是有效数值；`null`、空串、错误、重复目标记录和无法确认均不是 0。
- 不使用 `market_product_concentration`、Top 100 或其他商品样本降级。
- 24 个自然月是观察窗口，不是“必须有 24 个有效数值”的前提。缺失月份保留在数据明细中，标记缺失原因，但不纳入任何总和。
- 全部 24 个月有效时为 `ready`；存在至少一个有效月且存在缺失时为 `partial`；没有可确认月、类目无法确认、用户未确认未验证候选或连接不可用时为 `blocked`。
- 仅当“前 12 个月”和“近 12 个月”各自完整时，计算销量变化量与增长率。任一周期不完整时，可展示各自已确认月份合计和覆盖数，但增长率与变化量必须显示“—”。
- 仅当最近 3 个月及其前 3 个月均连续完整时，计算最近表现。峰值须注明“已确认月份内峰值”。

## 生成与校验

使用 [固定模板](assets/category-sales-trend-template.html)，仅通过 `scripts/build_dashboard.py` 注入数据并生成 HTML。模板的视觉、模块顺序、颜色、字体、栅格和离线 SVG 绘图方式不得重新设计。

输出文件名：`amazon_category_sales_trend_24m_<站点>_<目标截止月份>.html`。在可写的交付目录生成；无本地交付目录时按平台附件方式交付。

运行构建脚本前，准备符合模板数据结构的 JSON。脚本会验证月份顺序、有效数、状态、数值边界、覆盖率和 JSON 安全注入。生成后检查：

- 没有远程脚本、CDN、远程字体、远程图片或绝对本机依赖；
- 所有月度记录都属于目标窗口与最终类目；
- KPI、图表、洞察和表格使用同一份月度数据；
- `partial` 状态的缺失点不连接折线，不伪造增长率；
- 页面展示站点、目标截止月、数据覆盖数、类目路径和“类目确认状态”。对于用户确认的节点，必须原样展示“SellerSprite MCP 未提供叶子验证”，不得写成已验证叶子类目。

完成交付后，聊天中只提供 `跨境吴老师类目销量趋势看板已完成：<HTML 文件链接>`。启动、补充输入、连接选择或阻塞场景不受此限制。

## 公共发布边界

不要将用户商品名、ASIN、类目路径、节点 ID、真实销量、MCP 地址、密钥、令牌、调用日志或生成的 HTML 看板写入 Skill 资源或公开仓库。示例与测试只使用虚构商品和合成数据。

本 Skill 为跨境吴老师专用模板，未经授权不得移除、替换或弱化 Skill 名称、执行提示和页面标题中的跨境吴老师标识。
